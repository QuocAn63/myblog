export default function UserActionReducer(state, action) {
    switch (action.type) {
        case "LIKE":
            
            
            break;
    
        default:
            break;
    }
}